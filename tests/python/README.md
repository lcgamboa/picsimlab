
# PICSimLab blink 20ms Python test

This document outlines two implementations for testing the blink functionality on boards of PICSimLab:

##  Implementation 1: Using `unittest`

To run:
```
    python unittest_blink.py
```

## Implementation 2: Using `pytest`

To run:
```
    pytest -v --tb=no --no-header
```
