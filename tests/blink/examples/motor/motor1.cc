
#include"motor1.h"
#include"motor1_d.cc"

CPWindow1 Window1;

//Implementation


void
CPWindow1::button1_EvMouseButtonPress(CControl * control, uint button, uint x, uint y,uint state)
{
  //code here:)
  mprint(lxT("button1_EvMouseButtonPress\n"));
};

void
CPWindow1::button2_EvMouseButtonPress(CControl * control, uint button, uint x, uint y,uint state)
{
  //code here:)
  mprint(lxT("button2_EvMouseButtonPress\n"));
};

void
CPWindow1::button3_EvMouseButtonPress(CControl * control, uint button, uint x, uint y,uint state)
{
  //code here:)
  mprint(lxT("button3_EvMouseButtonPress\n"));
};

void
CPWindow1::button4_EvMouseButtonPress(CControl * control, uint button, uint x, uint y,uint state)
{
  //code here:)
  mprint(lxT("button4_EvMouseButtonPress\n"));
};

void
CPWindow1::button5_EvMouseButtonPress(CControl * control, uint button, uint x, uint y,uint state)
{
  //code here:)
  mprint(lxT("button5_EvMouseButtonPress\n"));
};

void
CPWindow1::button6_EvMouseButtonPress(CControl * control, uint button, uint x, uint y,uint state)
{
  //code here:)
  mprint(lxT("button6_EvMouseButtonPress\n"));
};

void
CPWindow1::button7_EvMouseButtonPress(CControl * control, uint button, uint x, uint y,uint state)
{
  //code here:)
  mprint(lxT("button7_EvMouseButtonPress\n"));
};

void
CPWindow1::button8_EvMouseButtonPress(CControl * control, uint button, uint x, uint y,uint state)
{
  //code here:)
  mprint(lxT("button8_EvMouseButtonPress\n"));
};

void
CPWindow1::button9_EvMouseButtonPress(CControl * control, uint button, uint x, uint y,uint state)
{
  //code here:)
  mprint(lxT("button9_EvMouseButtonPress\n"));
};

void
CPWindow1::button10_EvMouseButtonPress(CControl * control, uint button, uint x, uint y,uint state)
{
  //code here:)
  mprint(lxT("button10_EvMouseButtonPress\n"));
};

void
CPWindow1::button11_EvMouseButtonPress(CControl * control, uint button, uint x, uint y,uint state)
{
  //code here:)
  mprint(lxT("button11_EvMouseButtonPress\n"));
};












